This is a blank CI starter project that supports multiple sites running the same models/views.

It uses a custom Router and Controller class along with some nice to have libraries.

It also includes ion_auth and datamapper.